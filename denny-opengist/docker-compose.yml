version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-opengist_server_1
      APP_PORT: 6157
      PROXY_AUTH_ADD: "false"
  server:
    image: ghcr.io/thomiceli/opengist:1.8
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/opengist:/opengist