 version: "3.7"

 services:
   docker-ckpool:
     image: toconspiracy/docker-ckpool
     ports:
       - 3333:3333
     volumes:
       - /tmp/Arkinapool/:/tmp/Arkinapool/:rw
     restart: unless-stopped
