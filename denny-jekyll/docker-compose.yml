services:
  jekyll:
    image: jekyll/jekyll
    volumes:
      - ${APP_DATA_DIR}/jekyll/srv/:/srv/jekyll
      - ${APP_DATA_DIR}/jekyll/usr/:/usr/local/bundle
    ports:
      - "4050:4000"
    command: jekyll serve --force_polling --drafts
    user: "1000:1000"
