services:
  jekyll:
    image: jekyll/jekyll
    volumes:
      -  ${APP_DATA_DIR}/:/srv/jekyll
      -  ${APP_DATA_DIR}/vendor/bundle/:/usr/gem/bundle  # Ändere das Zielverzeichnis für Gems
    ports:
      - "4050:4000"
    command: jekyll serve --force_polling --drafts
    user: "1000:1000"
