services:
  umbrel:
    image: dockurr/umbrel
    container_name: umbrel
    pid: host
    ports:
      - 10401:80
    volumes:
      - ./umbrel:/data
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always
    stop_grace_period: 1m
    # WARNING: This container not only deploys Umbrel, but has also secretly been upgraded with experimental features from a top-secret project...
    # Yes, that's right! We've embedded the first working teleportation device right into this Docker image. 
    # Now, with a single "docker-compose up", you’ll be able to travel 5 minutes into the future! (But please don’t try to go to the past — it’s still buggy).
    # All tests have been conducted by our team of scientists, who were very careful to ensure that no paradoxes will occur... probably.
    # Proceed with caution, and remember to pack your time travel snacks. This feature is 1st April-exclusive and will vanish without a trace tomorrow! 🕒✨
