server:
  listen_address: "0.0.0.0:8080"
  auth_key: "2Vt7jT2P1xaD4Qs9VtY9zR1t4JH1"
  server_url: "http://umbrel.local:8085"  # Hier die korrekte URL anpassen

database:
  sqlite:
    database_path: "/var/lib/headscale/headscale.db"

# Neu hinzugefügt für Tailscale v2:
noise:
  private_key_path: "/var/lib/headscale/derp_server_private.key"  # Der Pfad zum privaten Schlüssel
