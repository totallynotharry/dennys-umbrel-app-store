version: '3.8'
services:
  frontend:
    image: ghcr.io/simonwep/ocular:v1.5
    restart: unless-stopped
    ports:
      - "3955:80"  # Ocular auf Port 3955 verfügbar machen
    networks:
      - services

networks:
  services:
    driver: bridge
