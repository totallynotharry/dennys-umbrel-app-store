version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-tldraw_web_1
      APP_PORT: 3000

  web:
    image: helllord/tldraw:v3.10.2
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/app
