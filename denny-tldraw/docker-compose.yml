version: '3.7'

services:
  app:
    image: ghcr.io/hpi-schul-cloud/tldraw-client:077e8c6dbddd549bd1e0dce3c3be93fbf380978a
    ports:
      - "8383:3046"
    volumes:
      - ./public:/usr/share/nginx/html/public
      - ./src:/app/src
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
