version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-gitingest_app_1
      APP_PORT: 8000

  app:
    image: elestio/gitingest:v0.1.3
    restart: on-failure
    environment:
      - ALLOWED_HOSTS=umbrel-2.local