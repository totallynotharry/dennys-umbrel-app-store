version: "3.3"
services:
  openvscode-server:
    image: gitpod/openvscode-server
    init: true
    ports:
      - 3783:3000
    volumes:
      - $(pwd):/home/workspace:cached
    restart: unless-stopped
