version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-scira_app_1
      APP_PORT: 3000

  app:
    image: lucasvnd/scira:latest
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: on-failure