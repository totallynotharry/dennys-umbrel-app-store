version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-bisq_app_1
      APP_PORT: 3389

  app:
    image: fflo/bisq-on-docker:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/bisq
