version: "3.9"

services:
  app_proxy:
    environment:
      APP_HOST: denny-chitchatter_web_1
      APP_PORT: 8299
      PROXY_AUTH_ADD: "false"

  web:
    image: noxcis/chitchatter:prion
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/:/data
