version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-cobalt_web_1
      APP_PORT: 9000
      PROXY_AUTH_ADD: "false"
  web:
    image: ghcr.io/imputnet/cobalt:10
    restart: on-failure
    environment:
        API_URL: "umbrel-2.local"
        API_PORT: "9000"
