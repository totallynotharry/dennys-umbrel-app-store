version: '3.9'

services:
  app_proxy:
    environment:
      APP_HOST: denny-actualbudget_server_1
      APP_PORT: 5006
      PROXY_AUTH_ADD: "false"

  server:
    image: docker.io/actualbudget/actual-server:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/actual-data:/data
