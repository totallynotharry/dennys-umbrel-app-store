version: "3.7"

services:
  enclosed:
    image: corentinth/enclosed
    ports:
      - 8525:8787
    volumes:
      - ${APP_DATA_DIR}/enclosed-data:/app/.data
    restart: on-failure

volumes:
  enclosed-data:
    driver: local
