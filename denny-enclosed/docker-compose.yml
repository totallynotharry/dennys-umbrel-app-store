services:
  enclosed:
    image: corentinth/enclosed
    ports:
      - 8525:8787
    volumes:
      - enclosed-data:/app/.data
    restart: unless-stopped

volumes:
  enclosed-data:
    driver: local
