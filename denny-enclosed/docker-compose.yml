services:
  enclosed:
    image: corentinth/enclosed
    ports:
      - 8525:8787
    volumes:
      - ${APP_DATA_DIR}/enclosed-data:/app/.data
    restart: on-failure
    environment:
      - APP_HOST=enclosed_web_1
      - PUID=1000
      - PGID=1000
