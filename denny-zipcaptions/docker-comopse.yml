version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: denny-zipcaptions_web_1
      APP_PORT: 9000

  excalidraw:
    image: educoder/zip-captions
    restart: on-failure
    stop_grace_period: 1m
